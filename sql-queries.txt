ALTER TABLE `transaction` ADD `coupon` VARCHAR(20) NULL DEFAULT NULL AFTER `expiry`;

ALTER TABLE `users` CHANGE `active` `active` ENUM('0','1','2') CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '0' COMMENT '0 pending,1 active,2 for reject';

ALTER TABLE `users` ADD `note` VARCHAR(255) NULL DEFAULT NULL AFTER `active`;

ALTER TABLE `users` CHANGE `note` `note` TEXT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NULL DEFAULT NULL;

ALTER TABLE `announcements` CHANGE `note` `note` TEXT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NULL DEFAULT NULL;


ALTER TABLE `coupons` ADD `coupon_for` ENUM('1','2') NOT NULL DEFAULT '1' COMMENT '1 is school,2 is tutor' AFTER `type`;


//12/09----------------------------------------------------------------
ALTER TABLE `subscriptions` ADD `days` INT NOT NULL DEFAULT '0' AFTER `type`, ADD `view_once` ENUM('0','1') NOT NULL DEFAULT '0' COMMENT 'this is for tutor who can see job once. o default for no, 1 for yes' AFTER `days`;

ALTER TABLE `subscriptions` ADD `user_type` ENUM('1','2','3') NOT NULL AFTER `amount`;

ALTER TABLE `transaction_subscriptions` ADD `days` INT NOT NULL DEFAULT '0' AFTER `type`, ADD `view_once` ENUM('0','1') NOT NULL DEFAULT '0' COMMENT 'this is for tutor who can see job once. o default for no, 1 for yes' AFTER `days`;


ALTER TABLE `transaction_subscriptions` ADD `user_type` ENUM('1','2','3') NOT NULL AFTER `amount`;